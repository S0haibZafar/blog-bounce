class BlogDTO {
    constructor(blog) {
        this.title = blog.title;
        this.author = blog.author;
        this.author = blog.author;
        this.photo = blog.photoPath;
    }
}

module.exports = BlogDTO;